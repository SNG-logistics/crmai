PORT=3001
DATABASE_URL="file:./lufy.db"
SESSION_SECRET="lufy-secret-change-this-in-production"
# Origin of the lufy frontend (used for CORS + session cookie).
# Runs on 3002 so it doesn't clash with the CRM frontend on 3000.
FRONTEND_URL="http://localhost:3002"
